/**
 * final constant
 *
 * used to calculate total length of physical thread needed for each color
 *
 * enum value is thread length needed per square/pixel
 *
 * one enum variable per thread count
 * (different length depending on base fabric thread counts)
 */

public enum ThreadNeeded {
}
